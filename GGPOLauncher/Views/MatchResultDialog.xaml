<Window x:Class="GGPOLauncher.Views.MatchResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resultado de la partida"
        Height="340"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F172A">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Confirma el ganador"
                   Foreground="#F9FAFB"
                   FontSize="26"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
                   x:Name="TxtPrompt"
                   Margin="0,12,0,0"
                   Foreground="#CBD5E1"
                   FontSize="14"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="2"
                    Margin="0,24,0,0">
            <Button x:Name="BtnKingWon"
                    Height="44"
                    Margin="0,0,0,12"
                    Background="#B45309"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Click="BtnKingWon_Click"/>
            <Button x:Name="BtnChallengerWon"
                    Height="44"
                    Margin="0,0,0,12"
                    Background="#1D4ED8"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Click="BtnChallengerWon_Click"/>
            <Button Content="Partida cancelada"
                    Height="42"
                    Margin="0,0,0,12"
                    Background="#475569"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BtnCancelled_Click"/>
            <Button Content="No estoy seguro"
                    Height="42"
                    Background="#7C3AED"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BtnNeedsReview_Click"/>
        </StackPanel>
    </Grid>
</Window>